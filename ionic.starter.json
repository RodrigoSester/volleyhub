{
  "name": "Sidemenu Starter",
  "baseref": "main",
  "tarignore": [
    "node_modules",
    "package-lock.json",
    "www"
  ],
  "scripts": {
    "test": "npm run build && npm i --no-save concurrently && ./node_modules/.bin/concurrently \"npm run dev\" --kill-others --success first"
  }
}
